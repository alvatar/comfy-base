# comfy-base

Shared Docker base image for ComfyUI projects. CUDA 12.8, Python 3.12, PyTorch 2.9.1, ComfyUI, flash-attn.

```bash
docker build -t alvatar/comfy-base:latest .
docker push alvatar/comfy-base:latest
```
